name: Add Or Update Alert Additional Data
description: 'The action adds or updates fields in the alert additional data. The
  result will be all accumulated data that was added to the alert. '
integration_identifier: Tools
parameters:
- name: Json Fields
  default_value: '{}'
  type: string
  description: You can enter either free text (for one variable) or a string 
    representing a JSON (Dict/List - Can be nested)
  is_mandatory: true
dynamic_results_metadata:
- result_name: JsonResult
  show_result: true
  result_example_path: 
    resources/AddOrUpdateAlertAdditionalData_JsonResult_example.json
default_result_value: ''
creator: Robert
script_result_name: ScriptResult
