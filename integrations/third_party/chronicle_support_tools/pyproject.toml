[project]
name = "ChronicleSupportTools"
version = "4.0"
description = ""
requires-python = ">=3.11,<3.12"
dependencies = [
    "pytest-json-report>=1.5.0",
]

[dependency-groups]
dev = [
    "integration-testing",
    "pytest>=8.3.5",
    "soar-sdk",
]

[tool.uv.sources]
soar-sdk = { git = "https://github.com/chronicle/soar-sdk.git" }
integration-testing = { path = "../../../packages/integration_testing" }
